POSTGRES_USER=foodies
POSTGRES_PASSWORD=changeme
POSTGRES_DB=postgres

# user-service
USER_SERVICE_DB_NAME=user_service_db

# user-service (auth)
# Générer une vraie valeur, ex: `openssl rand -base64 32` (>= 32 octets, Base64)
JWT_SECRET=changeme-generate-a-real-base64-secret-at-least-32-bytes
JWT_EXPIRATION_MS=0

# recipe-service
RECIPE_SERVICE_DB_NAME=recipe_service_db
USER_SERVICE_TIMEOUT_MS=5000
USER_SERVICE_RETRY_MAX_ATTEMPTS=3
